# Enable the daemon's socket for every user: `ryotunes` defers to the native
# client only while the socket exists, so without this a fresh install opens
# the Tauri app. Socket activation means nothing runs until something connects.
enable ryotunesd.socket
